Meshview
========

This project watches a MQTT topic for meshtastic messages, imports them to a
database and has a web UI to view them.

Example
-------
An example instance, <redacted>, is running with with data
from the MQTT topic msh/US/bayarea/#.


Running - Note, Some things I did to make life MUCH easier, and I am running this as raw python on a Ubuntu box.
-------

I chose to utilize a Ubuntu 20.04.6 LTS server. Any Linux server should work just fine, be sure to use the correct commands for package management (ie: yum, apt, etc.)

* Create a user in Ubunutu for Mesh: sudo useradd -s /bin/bash -d /home/mesh/ -m -G sudo mesh && sudo passwd mesh    (the -G sudo - can be ommitted if you don't want the user to have sudo capability)
* Install Python 3.12: $ sudo apt-get install python3.12 -y
* Create a bash source file to make life easier:  $ nano
   contents:
    export PYTHONPATH="$PYTHOMPATH=/usr/bin/python3.12"
    alias python="python3.12"
   save as: .bashrc
* load it by: $ source ~/.bashrc
* Install PIP: $ curl -O https://bootstrap.pypa.io/get-pip.py && python get-pip.py
* Clone repo: $ git clone <repo of choice>
* Be sure to edit: main.py to link to your mqqt feed of choice - In my config I am using: Server: mqtt.bayme.sh   Topic: msh/US/bayarea/#
* be sure to edit http.py - Config your server address.
* launch meshview via: $ python main.py

Now you can hit http://localhost:8080/



Notes:
-------

I have edited the base.html file to place a banner over every page for quick links to:  "See a realtime graph of the network"  - "See what people are saying"  - "See everything" - That Broke "Search for a node" - I'll circle back to that eventually.
You can change the web port by editing line 31 of main.py and chosing any port you like.  Be sure that port is open via whatever means you are running within (iptables, firewalld, Security Group in AWS, etc).
You may need to install an additional service if you are running this in a cloud host (Google Compute, AWS EC2, etc) to provide a transport from localhost to an external web port.  Nginx, etc...
Yes, this can be run in Windows 10+ utilizing WSL + Ubuntu. Directions for WSL+Ubuntu can be found easily online. You would still access the web ui in a browser via: http://localhost:8080 (or whatever port you've configured).
